"""
Provider client implementations.

This package contains client adapters for non-Claude providers that expose a
Claude-SDK-like async interface (query + receive_response) so the rest of the
backend can remain provider-agnostic.
"""

